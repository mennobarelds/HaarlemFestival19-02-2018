@model IEnumerable<WebApplication1.Models.Begeleider>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Naam)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Taal)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Naam)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Taal)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.BegeleiderId }) |
            @Html.ActionLink("Details", "Details", new { id=item.BegeleiderId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.BegeleiderId })
        </td>
    </tr>
}

</table>
